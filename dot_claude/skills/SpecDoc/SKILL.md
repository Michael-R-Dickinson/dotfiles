---
disable-model-invocation:
name: SpecDoc
description: A brief document specifying something at a high level
---
Ok lets document this in a specifications document. This isn't an implementation plan or TDD with things like action orderings or relevant code snippets for implementation - instead its about documenting the desired state. Essentially sections on each relevant component/piece of the system - what needs to exist, what already exists, what should be different. Make sure all information/decisions I've given you are included.
Also include overviews of the system for relevant sections before getting into the specifics of what is needed inside each component. This can be bulleted or numbered to show the sequence of operations. 
Begin the whole document with a brief high-level overview of the task.

Aim to be as brief as possible on the whole document while still capturing enough information to understand exactly what we want.

If you need extra implementation information that doesn't fit this format, put it in an appendix at the end - this section should be short.
