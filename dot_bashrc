DOTFILES_DIR="${DOTFILES_TMP:-$HOME}"

alias vim='vim -u $DOTFILES_DIR/.vimrc'

# Starship
if command -v starship >/dev/null 2>&1; then
    eval "$(starship init bash)"
fi

# Aliases
if [ -f "$DOTFILES_DIR/.shell_aliases" ]; then
    source "$DOTFILES_DIR/.shell_aliases"
fi
